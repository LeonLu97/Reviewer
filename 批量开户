批量开户功能特性梳理
1	描述(必选)：
UDC提供excel模板针对大批量开户行为进行操作，批量开户入口只能进行新增操作，不提批量修改和删除功能
2	场景(必选)：
有需要大批量操作开户的场景,界面入口如下

3	消息流程：
	数据从excel里解析到DTO
	DTO入库存储到数据库

页面action入口为doBatchOpenUser
 
支持03版本和2010版本，核心处理方法为creatUserInBatch
 
在process中实现自己表的读取方法，创建自己的UDCExcel对象存储到对应链表中
在startCreatUserInBatch实现自己新加的入库逻辑

4	Excel模板开发
4.1	解锁保护
如果提示没有权限修改工作表，需要在审阅里面点击取消保护，密码TD-tech
 
4.2	模板位置
 
三种模式，每个模式中英文两种，一次需修要改6个Excel模板
4.3	显示所有隐藏的sheet页
有些批量配置的表单是隐藏的，需要先全部显示出来，否则会数不对表单页，这个sheet页的序号和后面的校验规则以及代码是一一对应的，数对很重要。
在任意一个sheet上右键，有显示隐藏，里面的表单都依次恢复显示即可，要记住是哪些数完了还要隐藏回去。。。。。
 
这个页数和代码里的这个枚举是对应的，找到代码里最后一个枚举，在对应的sheet后面添加自己的sheet
 
注意：三种模式提供的批量开户功能不一样，而代码是一套，为了让不同的excel对齐同一套代码的枚举，表里面添加了这种隐藏的sheet做补位，不要随意删除。 
如我本次添加的窄带区域，表头复制别人的就可以了，格式保持统一，添加你自己需要的列。
4.4	添加输入信息提示
选取一整列，否则只会对当前单元格生效，点击数据>数据验证>输入信息标题与列名相同，提示信息根据需要填写。
 
效果，点击单元格时提示输入信息。
4.5	打开配置文件sheet
当前操作位打开隐藏的配置Sheet页，打开方法有三种：
1、直接双击showConfigSheet函数；
2、单机showConfigSheet函数，选择执行，就会发现sheet页栏会多出多个sheet页（就是我们的配置文件，只提供开发自身使用）；
4.6	配置校验规则
总的来讲clientRule为基本参数类型校验，serverRule为规则校验，可以校验是否唯一，是否为固定值等。
4.6.1	ClientRule页配置
ClientRule页配置输入值的校验规则，用来检查输入的值是否合法。
第一列 序号依次填，第二列版本号，更随大版本填，第三列表序号，自己数的那个，第四列，需要校验规则的列编号。第五第六列为校验的正则表达式，第七八列为语言资源，具体的校验规则看具体业务。
检查是否生效可在对应的列里输入非法字符，点击第一个sheet上的检查数据
 
如有错误会在错误sheet里显示。
 
4.6.2	ServerRule页配置
ServerRule页为多列联合校验，当某一列规则成立时，输入值是否允许为空，当前列是否唯一值，最大值等等。
玩法和第一张表差不多，跟着已有的填写即可。
4.7	踩坑指北
当前的excel模板里面有3个特殊的隐藏文件，在excel里面无法打开也无法看见，代码可以读到，所以宏定义的时候有6、7、8三个数没人用就是这个原因，数的时候在添加号段后面是有三张隐藏表的要注意。




